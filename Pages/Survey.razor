@page "/survey"
@using SkopeoWebsite.Components.UI

<PageTitle>Опросы - Skopeo Studio</PageTitle>

<div class="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-cyan-600 via-blue-600 to-indigo-600 text-white py-20 relative overflow-hidden">
        <div class="absolute inset-0 bg-black/10"></div>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
            <h1 class="text-5xl font-bold mb-4 bg-gradient-to-r from-white to-cyan-100 bg-clip-text text-transparent">
                Профессиональные Опросы
            </h1>
            <p class="text-xl mb-8 text-cyan-100">
                Собирайте данные и получайте полезную информацию с помощью наших инструментов
            </p>
            <Button Variant="Button.ButtonVariant.Secondary">
                Начать
            </Button>
        </div>
    </section>

    <!-- Features Section -->
    <section class="py-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h2 class="text-4xl font-bold mb-8 bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">
                Наши Услуги
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <Card class="bg-white/80 backdrop-blur-md border border-white/30 hover:scale-105 transition-all duration-300">
                    <CardHeader>
                        <div class="w-12 h-12 bg-gradient-to-r from-cyan-100 to-blue-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <Icon Name="clipboard" Size="h-6 w-6 text-cyan-600" />
                        </div>
                        <h3 class="text-xl font-semibold text-gray-900 mb-4">Создание Опросов</h3>
                    </CardHeader>
                    <CardContent>
                        <p class="text-gray-600">Легко создавайте опросы с помощью нашего интуитивно понятного интерфейса.</p>
                    </CardContent>
                </Card>
                
                <Card class="bg-white/80 backdrop-blur-md border border-white/30 hover:scale-105 transition-all duration-300">
                    <CardHeader>
                        <div class="w-12 h-12 bg-gradient-to-r from-purple-100 to-pink-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <Icon Name="chart" Size="h-6 w-6 text-purple-600" />
                        </div>
                        <h3 class="text-xl font-semibold text-gray-900 mb-4">Анализ Данных</h3>
                    </CardHeader>
                    <CardContent>
                        <p class="text-gray-600">Получайте глубокую аналитику и инсайты из собранных данных.</p>
                    </CardContent>
                </Card>
                
                <Card class="bg-white/80 backdrop-blur-md border border-white/30 hover:scale-105 transition-all duration-300">
                    <CardHeader>
                        <div class="w-12 h-12 bg-gradient-to-r from-emerald-100 to-green-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <Icon Name="users" Size="h-6 w-6 text-emerald-600" />
                        </div>
                        <h3 class="text-xl font-semibold text-gray-900 mb-4">Поддержка Клиентов</h3>
                    </CardHeader>
                    <CardContent>
                        <p class="text-gray-600">Наша команда всегда готова помочь вам в любое время.</p>
                    </CardContent>
                </Card>
                
                <Card class="bg-white/80 backdrop-blur-md border border-white/30 hover:scale-105 transition-all duration-300">
                    <CardHeader>
                        <div class="w-12 h-12 bg-gradient-to-r from-amber-100 to-orange-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <Icon Name="shield" Size="h-6 w-6 text-amber-600" />
                        </div>
                        <h3 class="text-xl font-semibold text-gray-900 mb-4">Безопасность Данных</h3>
                    </CardHeader>
                    <CardContent>
                        <p class="text-gray-600">Мы обеспечиваем высокий уровень безопасности для ваших данных.</p>
                    </CardContent>
                </Card>
            </div>
        </div>
    </section>

    <!-- How It Works Section -->
    <section class="py-20 bg-white/60 backdrop-blur-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h2 class="text-4xl font-bold mb-8 bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">
                Как Это Работает
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="group">
                    <div class="w-16 h-16 bg-gradient-to-r from-cyan-500 to-blue-600 text-white mb-4 rounded-full flex items-center justify-center text-lg font-bold mx-auto shadow-xl group-hover:scale-110 transition-transform duration-300">
                        1
                    </div>
                    <h3 class="text-xl font-semibold mb-2">Создайте Опрос</h3>
                    <p class="text-gray-600">Выберите тип опроса и добавьте вопросы.</p>
                </div>
                <div class="group">
                    <div class="w-16 h-16 bg-gradient-to-r from-cyan-500 to-blue-600 text-white mb-4 rounded-full flex items-center justify-center text-lg font-bold mx-auto shadow-xl group-hover:scale-110 transition-transform duration-300">
                        2
                    </div>
                    <h3 class="text-xl font-semibold mb-2">Распространите Его</h3>
                    <p class="text-gray-600">Поделитесь опросом с вашей аудиторией через различные каналы.</p>
                </div>
                <div class="group">
                    <div class="w-16 h-16 bg-gradient-to-r from-cyan-500 to-blue-600 text-white mb-4 rounded-full flex items-center justify-center text-lg font-bold mx-auto shadow-xl group-hover:scale-110 transition-transform duration-300">
                        3
                    </div>
                    <h3 class="text-xl font-semibold mb-2">Анализируйте Результаты</h3>
                    <p class="text-gray-600">Получите отчеты и визуализацию данных для принятия решений.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section class="py-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h2 class="text-4xl font-bold mb-8 bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">
                Отзывы Клиентов
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
                <Card class="bg-white/90 backdrop-blur-md p-6 rounded-lg shadow-xl hover:scale-105 transition-all duration-300 border border-white/30">
                    <CardContent>
                        <div class="flex items-center mb-4">
                            @for (int i = 1; i <= 5; i++)
                            {
                                <Icon Name="star" Size="h-5 w-5 text-yellow-400" Fill="currentColor" />
                            }
                        </div>
                        <p class="mb-4 text-gray-600">"Отличный сервис, который помог нам собрать необходимые данные!"</p>
                        <p class="font-semibold text-gray-900">— Иван Петров</p>
                    </CardContent>
                </Card>
                
                <Card class="bg-white/90 backdrop-blur-md p-6 rounded-lg shadow-xl hover:scale-105 transition-all duration-300 border border-white/30">
                    <CardContent>
                        <div class="flex items-center mb-4">
                            @for (int i = 1; i <= 5; i++)
                            {
                                <Icon Name="star" Size="h-5 w-5 text-yellow-400" Fill="currentColor" />
                            }
                        </div>
                        <p class="mb-4 text-gray-600">"Простота использования и отличная поддержка!"</p>
                        <p class="font-semibold text-gray-900">— Анна Сидорова</p>
                    </CardContent>
                </Card>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="py-20 bg-white/60 backdrop-blur-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h2 class="text-4xl font-bold mb-8 bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">
                Свяжитесь с Нами
            </h2>
            <p class="mb-8 text-gray-600">Готовы начать? Напишите нам!</p>
            <div class="flex flex-col sm:flex-row justify-center gap-4">
                <Button Variant="Button.ButtonVariant.Primary">
                    <Icon Name="mail" Size="h-4 w-4 mr-2" />
                    example@survey.com
                </Button>
                <Button Variant="Button.ButtonVariant.Secondary">
                    <Icon Name="phone" Size="h-4 w-4 mr-2" />
                    +7 (999) 123-45-67
                </Button>
            </div>
        </div>
    </section>
</div>
